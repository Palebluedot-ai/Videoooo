# requirements — Videoooo 需求三分法

> 三张清单：**已确认**（写死，AI 照做）/ **待拍板**（AI 不许替你决定）/ **暂停禁令**（未获明确指令前不实现）。
> 拍板即落盘：任何一条从「待拍板」挪到「已确认」，同批更新 `AGENTS.md` 相关表述与 `dashboard.html`。

## A. 已确认（2026-08-19 定档问答）

| # | 内容 |
|---|---|
| A1 | 项目性质 = **个人学习仓**：学 AI video，记学习笔记，跟着写相关代码。不是产品 |
| A2 | 交付物终局 = **本机自用工具**（这台 Mac 上跑，命令行为主） |
| A3 | 不碰钱 / 不碰真实用户数据 / 不对外服务 → 主车道 **Explore** |
| A4 | 时间尺度 = 持续迭代数周以上 → 档位 **M 标准**（不静默降档；变瘦要走 adopt §三，须你显式批准） |
| A5 | Python 包管理一律 `uv`，禁 pip（继承你的全局铁律） |
| A8 | **走代码路线**：不只当用户，要会写管线（2026-08-19 拍板）|
| A9 | **目标 = 做出一部精品短片**（高级感 / 工业级），掌握这个能力本身就是目的 |
| A10 | **预算不是约束**，但「不花冤枉钱」→ 撤销原 P2「不自动调付费 API」的逐次确认，改为记账制 |
| A11 | **主力模型 = Seedance**（BytePlus ModelArk 走国际版）。理由是硬能力：音画同步生成，本地开源模型给不了 |
| A12 | **参照物 = 《丧尸清道夫》**（Mx-Shell）。学：五段式提示词 + 设计层规避一致性。不复制：他的原始提示词文件（版权归他，本仓 public）|
| A13 | **本机 = MacBook Pro / M5 Max / 128GB / 40 核 GPU**（system_profiler 实读）→ 本地路线可行，但定位为 P4 可控性实验场，非出片主力 |
| A7 | 远程仓 = **public** `github.com/Palebluedot-ai/Videoooo`（2026-08-19 拍板）→ 触发 AGENTS §1「公开仓意识」硬约束 |
| A6 | 规划链（MRD/PRD/技术方案/autoplan）跳过 —— 学习仓无产品范围可写，已登记进 `agent-on.lock.md` local_deviations |

## B. 待拍板 —— ✅ 已全部结案（2026-08-19）

| # | 原问题 | 结论 |
|---|---|---|
| Q1 | 第一条主线工具 | **Seedance（云 API）为主力 + 本地 ComfyUI 作 P4 实验场**。不是三选一，是分工，理由见 [notes/01-learning-path.md](notes/01-learning-path.md) §四 |
| Q2 | 学习重心 | **会用 + 会调**——用五段式做出精品（会用），写批量管线与素材管理（会调）。不追「会改」（不训模型）|
| Q3 | 每月预算 | **不设上限**，但每次生成必须记台账（花费 + 抽卡次数 + 选没选中）|
| Q4 | 参照物 | **《丧尸清道夫》**——3 分 34 秒，10 天，3000 元，中专生做的 |

> 新的开放问题不再放这里，放 [notes/00-landscape.md](notes/00-landscape.md) 第九节「待查」。

## C. 暂停禁令（未获明确指令前不实现、不假设）

与 `AGENTS.md` §1 暂停项表同源，改一处两处同改：

| # | 禁令 | 解禁条件 |
|---|---|---|
| P1 | 不做产品化 / 不部署上线（无 Web 前端、无用户系统、无域名）。**注意：做出精品短片 ≠ 做产品**，片子可以发，工具不产品化 | 你显式说「要给别人用」→ 同时走 adopt §二升 L 档 |
| ~~P2~~ | ~~不自动调用付费 API~~ → **2026-08-19 解禁（D5）**：改为**记账制**——不再逐次确认，但每条生成必须落台账（模型/参数/抽第几次/花费/选没选中）。单次操作预计超 ¥200 仍须问一句 | — |
| P3 | 不碰真实用户数据 / 不采集第三方平台内容 | 你显式授权 + 升档 |
| P4 | 不引入重框架（Docker / K8s / **训练管线 / 微调** / 自建模型服务）。ComfyUI 本地推理**不算**重框架，P4 阶段可装 | 你点名 |
| P5 | **不把 Mx-Shell 原始提示词文件（.docx / prompts/）拷进本仓** —— 版权归他，本仓 public。只引用链接 | 取得他本人授权 |

## D. 决策台账

| 编号 | 日期 | 决策 |
|---|---|---|
| D1 | 2026-08-19 | 定档 M 标准 + Explore 主车道；规划链跳过并登记 deviation |
| D2 | 2026-08-19 | 新增「笔记真实性红线」为本项目头号硬约束：笔记内容必须来自实跑或当场查的官方文档，禁止 AI 凭记忆写 |
| D3 | 2026-08-19 | 远程仓建为 **public**（不是 private）→ 连带新增 AGENTS §1「公开仓意识」硬约束：密钥残迹/个人可识别信息/他人未公开素材不许落盘 |
| D4 | 2026-08-19 | 首轮 deep-research 收口：调研发现落 `notes/00-landscape.md`，学习路线落 `notes/01-learning-path.md` 并显式标注为「综合建议非调研发现」。新增纪律：**未经核实的 API 单价禁止写进笔记**（本轮 4 条价格声称被否 3 条）|
| D5 | 2026-08-19 | **Q1–Q4 全部结案**：走代码路线、目标做精品短片、Seedance 为主力（音画同步是硬理由）、本地 128GB 定位 P4 实验场、参照物《丧尸清道夫》。连带：P2 付费禁令解禁改记账制；新增 P5 版权禁令；`notes/01-learning-path.md` 整篇 v1 作废重写为作品驱动 P1–P4 |
| D6 | 2026-08-20 | **路径改按能力难度轴组织（v3）**，作废 v2 的作品驱动 P1-P4（那是生产流程轴）。第二轮 deep-research 的核心发现是否定结论：**公开资料里不存在能力阶梯**（7 条此类主张被 3 票否决），故自建 L0-L6，地基为 VBench-2.0 的 superficial/intrinsic 之分与模型崩溃维度数据。**证据等级逐条标注**：仅 eval 一轴被证实已系统论述，prompt/context/harness/graph 四轴标为「我的类比」|
| D7 | 2026-08-20 | **补上 SOP 缺口**（用户指出「要的是方法不是测试，工业级 SOP 怎么分镜、三视图之类的你都没有」）。新增 `notes/05-industrial-sop.md`：8 阶段流水线 + 2 道人工闸 + 4 条硬限制、beat map 四件套、**5-Aspect Shot Breakdown**（强制分镜规范，Camera 书写顺序被规定死）、character sheet 六件套与 turnaround 五视图标准、AI 生成 turnaround 的 prompt、资产三条防烧钱规则、合成层保护项。**L0 由「拉片测试」改为「前期设定」**；12 破绽降级为贯穿全程的验收 QC 门 |
| D8 | 2026-08-20 | **补上学习法这一层**（用户：「不是一步一步陪我把 hero 镜头填出来，是一个 SOP 和教程，我应该怎么学习，先不训练」）。新增 `notes/06-how-to-study.md`：读序（SOP 必须先于五段式）、学习循环五步（认知差是最值钱的一步）、**反推法**（拿成片当教材反推分镜表——不生成一帧、有标准答案、直接练 L0）、学习台账、升级判据（判据过 + 能教会别人）、三个学习期误区。**当前状态定为「学习期」**，第七节给了不训练版六步 |
| D9 | 2026-08-20 | **参考文档教学化**（用户：「这些文档写的非常详细，做一个给小白的 AI 视频教程，配合一些练习（可跳过）呈现出来」）。产出 `notes/course.html` 入门六课：按既定读序组织（地基 → 流水线 → 分镜 → 设定集 → 阶梯 → 五段式），术语首次出现均释义（t2v/i2v/抽卡/景别/运镜/景深/POV/overlay），每课带「学完你能」+ 可跳过练习（写明跳过代价）+ 自查。**未再跑一轮 deep-research**——两轮已把料挖齐并对抗验证，剩余缺口需定向轮，本轮瓶颈是教法不是资料 |
| D10 | 2026-08-20 | **会话级排程落定**（用户：「初学者怎么一步一步开始、怎么步步推进」）。用 3 方案对抗（产出/闸/动力驱动各一份，逐份证伪，全部 needs-major-fix）后合成 `notes/07-study-runway.md`：15 节 / 7 道闸 / 判据三档（🟩机器/🟨客观人工/🟥自评）/ 防停滞六条。**连带修复 check_shotlist v1 两个静默放行洞**（模板逐字副本可全绿过门；camera 写成 YAML 字典时检查整段跳过——均已亲手复现），v2 加模板残留检测 + dict 支持 + --strict，经 tools/fixtures/ 8 例正反验证。承认此前「正反两向已验」的声明验证不完整。注：4 个审计 agent 因连接中断失败，由证伪 agent 的实测（含真跑命令）补位 |
| D11 | 2026-08-20 | **学习制度两次纠偏定稿**：①用户「完全没概念，没办法自己验收」→ 验收一律归 AI，写进 AGENTS；②用户「不用互动式给我安排任务，先设计路径，给我大框架不断完善」→ 撤回陪学派任务协议，立 `docs/ROADMAP.md` 总框架 v1（产出物倒推：M1 主播诞生 → M2 第一集样片 → M3 生产手册 → M4 一条命令出一集 → M5 日更系统），框架本身是迭代对象。能力阶梯降为随行知识线。唯一挡路决定 = F1 主播形象 |
