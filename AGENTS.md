<!-- instantiated-from kit/AGENTS-skeleton.md @ v0.18.0 -->
# AGENTS.md — Videoooo 行为宪法

> Agent 在本仓的最高规则。与人类沟通语言跟随用户（默认中文）；本文件条款冲突时，越靠前越优先。

## §0 agent-on 映射

方法论来自 agent-on（版本与偏离登记见项目根 `agent-on.lock.md`，只映射不复制）。口令：「agent-on 结账」（沉淀回流）/「agent-on 升级」（bump pin）/「整理想法」/「更新仪表盘」。**学习执行原则**：主框架见 `docs/ROADMAP.md`（产出物倒推，迭代框架本身）；**验收一律归 AI**——不许设计要用户自己验收的流程。

**本项目是什么**：Chao 学习 AI video 的个人学习仓 —— 一边记学习笔记，一边跟着写 AI video 相关的代码实验。**不是产品**，交付物终局 = 本机自用工具。档位 **M 标准**（持续迭代数周以上），主车道 **Explore**。

## §1 硬约束（违反=事故）

| 约束 | 内容 |
|---|---|
| **笔记真实性红线**（本项目头号红线） | 写进 `docs/notes/` 的模型名、API 参数、价格、能力边界、代码片段，**必须**来自①实跑输出 或 ②当场查到的官方文档（带链接+查阅日期）。**禁止凭模型记忆写笔记**——AI video 领域半年就换代，凭记忆写 = 用户学到假知识，比不写更糟。拿不准就写「⏸ 待验证」并注明怎么验 |
| **代码即证据** | 笔记里贴的每段代码，要么在 `docs/experiments/` 有可跑的对应文件 + 实际输出，要么显式标「未跑通·伪代码」。禁止贴一段看起来能跑的代码却从没执行过 |
| **密钥红线** | 视频/图像生成 API key（Runway / Pika / Veo / Sora / Replicate / fal 等）只进 gitignored `.env`；**禁入**笔记正文、commit、日志、截图。代码一律 `os.environ[...]` 读，不硬编码 |
| **大文件红线** | 生成的视频/音频/模型权重/数据集**不入 git**。`.gitignore` 已挡 `*.mp4/mov/webm/png` 等；确要留证据就留缩略图或首帧截图（≤200KB）并解释 |
| **Python 包管理** | 一律 `uv`（`uv venv` / `uv pip install` / `uv run`）。**禁止 pip / pip3 install**（用户全局铁律） |
| **公开仓意识** | 本仓是 **public**（github.com/Palebluedot-ai/Videoooo）。写进任何文件的内容默认全世界可见：密钥/账号残迹、可识别个人信息、他人未公开素材 **一律不许落盘**；实验截图入库前先看一眼有没有带 key 的终端行或私人路径。花费金额可写（对读者有价值），账号标识不可写 |
| **不 vendor 版权素材** | Mx-Shell《丧尸清道夫》原始提示词、他人未公开授权的模板/素材，**只引用链接，不拷进本仓**（版权归原作者，本仓 public）。学方法论可以，搬文件不行 |
| **不写死暂停项** | 见下表；未获明确指令前不实现、不假设 |
| 外向操作 | push / 建远程资源 / 调**付费** API，首次须用户确认。假定一切 CLI 在非交互环境自动确认 |
| **runtime ≠ product surface** | 本项目 runtime 与交付面重合（都在本机）；若将来某个实验需要云跑（GPU/长渲染），那是 runtime 变化，**不等于**项目要产品化——不得据此自动升级成部署项目 |

### 暂停项清单（= 禁令，不是「以后顺手做」）

| # | 暂停项 | 解禁条件 |
|---|---|---|
| P1 | **不做产品化 / 不部署上线**（无 Web 前端、无用户系统、无域名） | 用户显式说「要给别人用」→ 同时走 adopt §二升 L 档 |
| P2 | **不自动调用付费 API** | 每次调付费接口前问一句、报预估花费；用户点头才跑 |
| P3 | **不碰真实用户数据 / 不采集第三方平台内容** | 用户显式授权 + 升档 |
| P4 | **不引入重框架**（不上 Docker / K8s / 训练管线 / 自建模型服务） | 用户点名 |

## §2 纪律四件套

1. **完成 = 贴命令实际输出**：禁止「应该能跑」「理论上」。学习仓尤其致命——没跑过的结论不是知识。
2. **Error Signal 四要素**：报障必含 What / Where / How（复现）/ Severity。
3. **单一状态写者**：`docs/state/progress.yaml` 只有 orchestrator（主会话）写。
4. **TDD 按车道分**：Explore 车道（跑实验、试模型、写笔记）**不强制写测试**；一旦某段代码变成复用工具（进 `tools/`），转 Ship 车道，补测试再用。

**提交纪律**：声明原子提交前 `git status --short` 读全暂存区。

**扫坑指针**：接新外部服务（新的视频 API / SDK）前，先对照 agent-on `bench/cases/README.md` 使用时机表。

## §9 动态需求协议（用户中途提新想法时）

① 复述确认边界 → ② 定位置（本 phase / 新 phase / 暂停项）→ ③ 更新对应文档 → ④ 继续主线。
**想法类捷径**：用户随口冒出的想法/待办（非 debug、非状态询问），AI 当场代笔一行进 `thoughts-and-ideas.md` 📥速记区（带日期+「对话捕获」标），口头确认一句即继续——只进速记区，升级成需求由用户拍板。拿不准就不记，宁漏勿噪。

## §skill 路由（制度在 agent-on；环节 skill 可选）

| 环节 | 本项目默认 | 说明 |
|---|---|---|
| 规划 | 主会话 + 用户拍板 | 学习项目不走 MRD/PRD 链（见 lock 的 local_deviations） |
| 实现执行 | **主会话直接做** + 完成贴证据 | 不默认开子代理 |
| 调试 | GStack `/investigate`（已装） | fallback：完成贴证据 + 系统性排障 |
| 审查 | 一般不需要；要用则 GStack `/review` | 学习代码不走 PR 流程 |

**压制条款（防抢跑·必守）**：本文件是 Claude Code 与 Codex 的共读层。
- 禁用 `superpowers:brainstorming` / `superpowers:writing-plans` 抢跑 init 与规划
- 实现**不**默认 `superpowers:subagent-driven-development`（用户点名才用）
- 不并行再开第二套审查/调试制度

## §二车道

**Explore（本项目默认）**：跑实验、试模型、试 prompt、写笔记——一把梭，不写测试，产物可丢弃。
**Ship（例外）**：某段代码要变成反复用的工具（进 `tools/`），或碰付费/密钥/外部服务——补测试、贴证据、走全纪律。
**两道不许串**：Explore 里的脚本直接提拔成常用工具前，必须重写一遍并补测试。
